<Window
    x:Class="CMP2.MainWindow"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:Controller="clr-namespace:CMP2.Controller"
    xmlns:model="clr-namespace:CMP2.Core.Model"
    xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
    TextElement.Foreground="{DynamicResource MaterialDesignBody}"
    Background="{DynamicResource MaterialDesignPaper}"
    TextElement.FontWeight="Medium"
    TextElement.FontSize="14"
    FontFamily="{Binding FontFamily}"
    mc:Ignorable="d"
    Title="CustomMediaPlayer - V2"
    WindowStartupLocation="CenterScreen"
    WindowStyle="None"
    Width="800"
    Height="450"
    MinWidth="500"
    MinHeight="400">

    <Window.DataContext>
        <model:ViewModelBase />
    </Window.DataContext>

    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition
                Width="*" />
            <ColumnDefinition
                Width="50" />
        </Grid.ColumnDefinitions>
        <Grid.RowDefinitions>
            <RowDefinition
                Height="50" />
            <RowDefinition
                Height="*" />
        </Grid.RowDefinitions>

        <Button
            x:Name="ExitButteon"
            HorizontalAlignment="Stretch"
            VerticalAlignment="Stretch"
            Style="{DynamicResource MaterialDesignIconButton}"
            Content="{materialDesign:PackIcon Kind=Shutdown, Size=30}"
            Panel.ZIndex="999"
            Cursor="Hand"
            Grid.Column="1"
            Grid.Row="0"/>

        <Grid
            Grid.Column="0"
            Grid.ColumnSpan="2"
            Grid.Row="0"
            Grid.RowSpan="2">
            <Grid.ColumnDefinitions>
                <ColumnDefinition
                    Width="*" />
            </Grid.ColumnDefinitions>
            <Grid.RowDefinitions>
                <RowDefinition
                    Height="*" />
                <RowDefinition
                    Height="Auto" />
            </Grid.RowDefinitions>

            <Controller:MediaInfoControl
                x:Name="MainInfoControl"
                Grid.Column="0"
                Grid.Row="0"
                Grid.RowSpan="2" />
            <Controller:MediaControllerControl
                x:Name="MainControllerControl"
                Visibility="Collapsed"
                Grid.Column="0"
                Grid.Row="1" />
            <!--<Controller:PlayListControl
            x:Name="MainPlayListControl"
            Grid.Column="1"
            Grid.Row="0"
            Grid.RowSpan="2"/>-->
        </Grid>
    </Grid>
</Window>